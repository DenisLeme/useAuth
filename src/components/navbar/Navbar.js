"use client"
import "./style.css"

export default function Navbar(){

    return(
        <nav className="navbar-style">
            <span>Bem-Vindo</span>
            <button className="button-navbar"
            >Logout</button>
        </nav>
    )
}